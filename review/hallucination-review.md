# Hallucinated endpoints / cmdlets / parameters / CRDs — adversarial review

Target: `/home/claude/vcf-skills/skills/` (4 skills, 21 files, 7,816 lines).
Ground truth, in priority order: `research/spec-inventory/*.ops.json` + `index.json`;
raw specs at `/tmp/vcf-api-specs` (tag 9.1.0.0) and `/tmp/vcf-specs-90` (tag 9.0.0.0);
`research/*.md` dossiers.

**Scope of verification actually performed**

| Class | Items checked | Method |
|---|---|---|
| API path strings | 312 distinct (all occurrences, all 4 skills + evals.json) | machine cross-check against every `*.ops.json`, with base-path normalization |
| operationIds cited by name | 133 | exact lookup in the 9.1 NSX / SDDC-Manager / fleet-lcm / sddc-lcm / vcf-operations inventories |
| Operation counts, base paths, securitySchemes, spec_file, openapi version | 30 product×version records | `index.json` + recomputed from `.ops.json` |
| Added / removed / newly-deprecated counts | 7 products | recomputed set diff 9.0 vs 9.1 |
| Payload field names, enums, defaults, required flags | ~70 (NSX `Rule`, `BaseRule`, `SecurityPolicy`, `Policy`, `Group`, `Condition`, `Expression`, `PathExpression`, `ServiceEntry`, `L4PortSetServiceEntry`, `ApiServiceConfig`; SDDC-Manager `System`, `ApplianceInfo`, `Release`, `SddcManager`) | grep/slice of raw YAML/JSON |
| PowerCLI modules, cmdlets, parameters | 34 | `research/tooling-powercli-vks-sdk.md` |
| kubectl / CRD / VKS CLI items | 18 | `research/tooling-powercli-vks-sdk.md`, `research/foundation-auth-identity.md` |
| Doc-portal slugs & URL patterns | 27 | `research/SOURCE-INVENTORY.md` + dossier source indexes |
| **Total** | **~560** | |

---

## Findings

| File | Line-ish | Claim | Version claimed | Verdict | Evidence | Severity |
|---|---|---|---|---|---|---|
| `vcf-foundation/SKILL.md` | 36 | "`GET /v1/system` on SDDC Manager returns the VCF version" | 9.0 + 9.1 (version-detection step 1) | **MISGRADED** — path exists, stated behavior is false | `9.{0,1}__sddc-manager.ops.json` → `GET /v1/system` = `getSystemConfiguration`, response `$ref: #/components/schemas/System`. `System.properties` = `id`, `maxAllowedDomainsInSubscription`(, `vcfInstanceName` in 9.1 only). **No version field in either tag.** Version lives in `Release.version` (`GET /v1/releases/system`, `getSystemRelease`), `ApplianceInfo.version` (`GET /v1/system/appliance-info`, `getApplianceInfo`), `SddcManager.version` (`GET /v1/sddc-managers`) | **blocker** |
| `vcf-foundation/references/9.0/auth-and-identity.md` | 196, 373 | Session ops table: "`POST /api/cis/session` (create), `GET /api/cis/session`, `DELETE /api/cis/session`" — stated as fact, no UNVERIFIED tag, no inline ambiguity note | 9.0 | **NOT FOUND** | `/api/cis/session` appears nowhere in `vsphere-automation` at either tag. The spec declares `servers[0].url: https://{host}/api` + path `/session` → `POST /api/session`. `/tmp/vcf-specs-90/…/automation/vcenter.yaml`, `Cis.Session_create` / `_get` / `_delete`, `security: [basic_auth]`, 201 returns a token string | **major** |
| `vcf-foundation/references/9.0/auth-and-identity.md` | 595–599 (§7 item 5) | "Neither `/cis/session` nor `/session` appears as an operation in the `vsphere-automation` spec at either tag — only content-library `download-session`/`update-session` operations match" | 9.0 (+9.1) | **NOT FOUND** (the negative claim is false) | `GET|POST|DELETE /session` = `Cis.Session_get|_create|_delete`, present at **both** `9.0.0.0` and `9.1.0.0`. The spec **does** resolve the prose ambiguity — in favour of `POST /api/session` | **major** |
| `vcf-foundation/references/9.1/auth-and-identity.md` | 274–278; 789–792 (§8 item 5); 812 | Same false spec-negative, repeated; `/api/cis/session` left as a live candidate | 9.1 | **NOT FOUND** | as above | **major** |
| `vcf-foundation/references/deltas.md` | 87 | "vCenter session path … the spec contains **neither**" | 9.0→9.1 | **NOT FOUND** | as above | **major** |
| `nsx-security-policy/references/9.1/dfw.md` | 232–233 | "Per-client rate limit **100 req/s** … per-client concurrency **40**. **[DOC + SPEC — the 9.1 spec's `api-service` example shows `"client_api_rate_limit": 100`, `"client_api_concurrency_limit": 40`]**" | 9.1 | **MISGRADED** — the `[SPEC]` half is fabricated and contradicts the spec | `nsx_policy_api.yaml` L93481-93486 `x-vmw-nsx-example-response` for `GetApiServiceConfig` shows `"client_api_rate_limit": 250`, `"client_api_concurrency_limit": 100`; `ApiServiceConfig` schema defaults are `250` / `100`. All 3 example blocks in the file show 250/100. The 100/40 figures are 9.1 **prose** only (`research/nsx.md` L310-313, [S18]) | **major** |
| `nsx-security-policy/references/deltas.md` | 133 | Same fabricated example, tagged `[ASYMMETRIC]` | 9.0→9.1 | **MISGRADED** | as above | **major** |
| `nsx-security-policy/references/lookup.md` | 250 | "Per-client 100 req/s and 40 concurrent" presented alongside the spec `global_api_concurrency_limit: 500` | 9.0+9.1 | **MISGRADED** (weaker: no explicit `[SPEC]` tag on the 100/40) | as above | minor |
| `nsx-security-policy/references/9.1/dfw.md` | 203–205 | "`ApiServiceConfig.session_timeout` has `default: 1800` … **and the spec's example `api-service` payload shows `"session_timeout": 1800`**. **[SPEC]**" | 9.1 | Schema default **CONFIRMED**; example claim **NOT FOUND** | `session_timeout: default: 1800` confirmed in `ApiServiceConfig`. The `api-service` example payload (L93481-93509) contains no `session_timeout` key at all | **major** |
| `vcf-api-discovery/references/spec-corpus.md` | 96–110 | "A machine-extracted operation inventory **ships with this skill**: `references/spec-inventory/{index.json, SUMMARY.md, DELTA-…, *.ops.json}`" | both | **NOT FOUND** | `skills/vcf-api-discovery/references/` contains only `doc-portal.md`, `live-discovery.md`, `spec-corpus.md` and two **empty** dirs `9.0/`, `9.1/`. No `spec-inventory/`. `find_operation.py` only runs because its 3rd fallback candidate reaches `../../../research/spec-inventory` — i.e. the skill is inoperable if packaged/installed on its own | **major** |
| `vcf-api-discovery/SKILL.md` | 46–48 | "**six** products have no spec at the 9.0 tag — all three NSX specs, fleet-lcm, sddc-lcm, realtime-metrics and log-management" | 9.0 | **MISGRADED** (count) | The enumeration lists **seven**; `index.json` confirms 7 absent at 9.0. Script's `ONLY_9_1` tuple correctly has 7 entries | minor |
| `vcf-api-discovery/references/spec-corpus.md` | 135 | "**Six** product keys are absent from the 9.0.0.0 tag" over a 7-row table | 9.0 | **MISGRADED** (count) | same | minor |
| `vcf-api-discovery/SKILL.md` | 3 (description) | "across roughly **13,000** operations in vSphere, NSX, SDDC Manager, VCF Operations, **VCF Automation**, vSAN, fleet lifecycle and **VKS**" | 9.0/9.1 | **MISGRADED** | Corpus totals: 9.1 = 11,590 ops / 14 specs; 9.0 = 5,083 / 8 specs (both recomputed and matching the file's own §2). **No VCF Automation spec and no VKS spec exists at either tag** — naming them inside the operation-count claim overstates corpus coverage of exactly the two products the dossiers flag as unspecced | minor |
| `nsx-security-policy/references/9.1/dfw.md` | 456 | `Rule.scope` — "Defaults to the policy's scope if omitted", inside a table headed `**[SPEC]**` | 9.1 | **MISGRADED** (grade, not fact) | `BaseRule.scope` description in the spec says only *"The list of policy paths where the rule is applied…"*; the inheritance behavior is not spec-stated. Operationally plausible, but it is not `[SPEC]` evidence | minor |
| `vcf-api-discovery/references/` | — | Empty `9.0/` and `9.1/` directories | — | packaging cruft | `ls -R` | minor |

---

## Items checked and CONFIRMED (no defect) — summary

Everything below was verified positively and is recorded so the review is auditable.

**Named traps from the brief — all clean:**

| Trap | Verdict |
|---|---|
| `VCF.PowerCLI` vs `VMware.PowerCLI` | CONFIRMED correct in all 5 places; `VMware.PowerCLI` only ever named as the pre-9.x name. `research/tooling-powercli-vks-sdk.md` L19-34 [S12][S05] |
| `Connect-VcfSddcManagerServer` + `-IgnoreInvalidCertificate` | CONFIRMED. Full parameter set in `powercli-session.md` §4.2 is verbatim identical to the dossier L143-154 [S20], incl. `-Password <SecureString>`, `-Protocol` default HTTPS, and the correct negative "reference page does not list `-VcfApiToken`/`-VcfOAuthSecurityContext`" |
| `Connect-NsxtServer` / `Connect-CisServer` | CONFIRMED correctly graded — both listed under "Cmdlets the research could NOT verify" (`powercli-session.md` §5) exactly as the dossier marks them (`## Gaps and Ambiguities` item 11). The file also flags the `Connect-NsxServer` vs `Connect-NsxtServer` name conflict between dossiers |
| `vcf context create` vs `kubectl vsphere login` | CONFIRMED. `vcf context create` taught in both auth files; `kubectl vsphere login` appears only as the explicit "Trap: the login step is `vcf`, not `kubectl vsphere`". `VCF_CLI_VSPHERE_PASSWORD`, `VCF_CLI_VCFA_API_TOKEN`, `--api-token`, `--tenant-name`, `--ca-certificate` all match `foundation-auth-identity.md` L36-37 [S33][S34] |
| `/iaas/api/...` for VCF Automation | CONFIRMED ABSENT — zero occurrences anywhere in `skills/`. The dossier's "never confirmed" note (`vcf-automation.md` L560) was respected |
| `/api/esx/settings/` or `/api/esx-settings/` | CONFIRMED ABSENT — zero occurrences anywhere in `skills/` |
| `GET /v1/system` existence | CONFIRMED in `sddc-manager` at **both** tags (`getSystemConfiguration`) — but see the blocker above re: what it returns |
| `GET /api/v1/spec/openapi/nsx_policy_api.json` | CONFIRMED against `research/nsx.md` L159-160 [S17] (both 9.0.0 and 9.1.0 API guides); the `nsx_vmc_policy_api` / `nsx_vmc_aws_integration_api` variants in `live-discovery.md` also trace to the dossier L674-679 |

**NSX 9.1 (`nsx-security-policy/references/9.1/dfw.md`, `deltas.md`, `lookup.md`)** — 91/91 cited operationIds resolve in `9.1__nsx-{policy,manager,global-policy}.ops.json` with the exact method and path stated: session (`CreateAuthenticatedSession`, `DestroyAuthenticatedSession`, correctly noted as absolute paths outside `/api/v1`), api-service (`Get|UpdateApiServiceConfig`), domains, all group CRUD + `member-types` + `members/virtual-machines`, all 12 `*-expressions/{expression-id}` incremental ops, all security-policy and rule CRUD + `?action=revise` + `/statistics`, all 9 draft ops, the whole `/infra/settings/firewall/**` family, `GetFilteredPolicies`/`GetFilteredRules`, `GenerateHostConfigReportInCsv`, IDFW, schedulers, session-timer profiles, context-profiles, services, `ReadInfra|PatchInfra|UpdateInfra`, `QuerySearch`/`DslSearch`/`SearchReconcile`, all VPC- and project-scoped variants, all `GlobalInfra*` variants including the read-only-on-local-manager split, and all 10 port-mirroring ops. `communication-maps` = exactly 12 ops, **all** `deprecated: true` — as claimed. `firewall-identity-stores` = 18/26 deprecated ("largely deprecated" — accurate); the 4 named non-deprecated survivors all resolve.

**NSX schemas** — `Rule.action` enum (`ALLOW|DROP|REJECT|JUMP_TO_APPLICATION`), all 17 `BaseRule` fields with correct defaults (`direction: IN_OUT`, `disabled: false`, `logged: false`), `ip_protocol` enum, the `"ANY"` literal on `source_groups`, `L4PortSetServiceEntry` (`required: [l4_protocol]`, `TCP|UDP`, `destination_ports`/`source_ports`), all 7 `ServiceEntry.resource_type` values, all 9 `Expression.resource_type` values, `Condition.required = [value, key, member_type]` and its full 11-value `key` enum (incl. `PodCidr`, `ManagementInterface`), the operator enum, `PathExpression.required = [paths]`, `Group` fields + `state` enum, all 13 `Policy` fields, `SecurityPolicy.{rules, connectivity_preference, connectivity_strategy, logging_enabled, default_rule_id, application_connectivity_strategy}` with both enums verbatim, the five-category verbatim quote, `?action=revise` `operation`/`anchor_path` params, `cookie_based_authentication_enabled` (`default: true`) with its verbatim description, `global_api_concurrency_limit: 500`, `session_timeout: 1800`, and the `/api/session/create` `x-vmw-nsx-example-request`/`-response` blocks (`j_username=admin&j_password=my-password`; `set-cookie: JSESSIONID=…; SameSite=Lax`) — **all verbatim-correct**.

**NSX 9.0 (`references/9.0/dfw.md`)** — the `[DOC-9.0]` / `[9.1-ONLY — NOT VERIFIED FOR 9.0]` / `[INFERRED]` grading is faithful to `research/nsx.md` §4 [S29][S31] line-for-line. Every path graded `[DOC-9.0]` appears in the dossier's 9.0.0-pinned table; every path graded `[9.1-ONLY]` (group list, group write verbs, `*-expressions`, `/infra/services`, `/infra/context-profiles`, `/infra/firewall/{policies,rules}`, `/search/*`, policy-level `/statistics`, project-scoped security-policies, VPC security-policies, draft list/`/complete`/`/aggregated*`, host-configuration-report) is genuinely absent from the 9.0 dossier table. Build numbers 24733065 / 25318225 match [S3][S4]. 100/40/199 rate limits correctly tagged `[DOC-9.0]` here (only the 9.1 file mis-tags them).

**SDDC Manager lifecycle** — 71/71 paths in `9.0/lifecycle.md` resolve in `9.0__sddc-manager.ops.json` with the stated verbs, **including every deprecation claim**: `POST /v1/bundles`, `GET|POST /v1/product-version-catalog`, all `version-aliases`, `GET /v1/identity-broker/prechecks`, `GET /v1/identity-broker/sddc-manager-oidc`, `POST /v1/nsx-alb-clusters/validations/version` — all `deprecated: true` at 9.0 as claimed. `/v1/system/precheck` genuinely absent from both tags, and "no precheck operation in the VCF Operations spec either" independently reconfirmed.

**SDDC Manager 9.1** — 46/46 checked paths resolve; every "new in 9.1" flag verified by set-diff (`/v1/version-drift`, `upgrade-sequences`, `vcenter-sizing-infos`, `vcenter-upgrade-mechanisms`, `PATCH /v1/sddc-manager`, `/v1/sddc-manager/validations[/{id}]`, `vcf-management-components/{passwords,resources-calculation,vcfops-discovery}`, `image-compliance/queries`, `clusters/{id}/remediations`, `hosts/{id}/software`, `depot/machine-details`, the 3 check-set `exports` ops). **The "21 newly deprecated" list is exactly right** — recomputed set diff yields precisely 21, and the enumerated families (`/v1/edge-clusters`, `PATCH /v1/domains/{id}/overlay`, `system/dns-configuration`, `system/ntp-configuration`, the upgrade-precheck pair) are exactly those 21.

**fleet-lcm / sddc-lcm** — all 51 and all 26 operations listed in `9.1/lifecycle.md` and `upgrade-runbook.md` match the inventories **exactly**, including the `?action=` query-verb forms (`?action=sync|configure|precheck|apply|cancel|retry`), the absence of an upgrade-plan resource on sddc-lcm, and both security-scheme sets (`basicAuth`+`bearerToken` / `bearerToken` only).

**VCF Operations** — all 43 checked IAM/cert/auth/token paths resolve; `iam/**` = 0 ops at 9.0 and exactly **70** at 9.1; `/auth/*` = 57 at 9.0 / 59 at 9.1; certificate ops = 5 at 9.0 / **24** at 9.1; `token/exchange` and `sources/vidb/well-known-url` genuinely 9.1-only; 0 dashboard paths at either tag (as claimed).

**Counts and metadata** — every operation count, base path, `securitySchemes` set, `spec_file` and OpenAPI version in `spec-corpus.md` §2 and in the two auth files matches `index.json` exactly. Corpus totals (14 specs / 11,590 ops; 8 specs / 5,083 ops) sum correctly. Per-product added/removed recomputed and matching: sddc-manager 48/0, vcf-operations 134/0, vcf-installer 5/0, vsphere-automation 101/9, vsphere-vi-json 48/0, vsan-dp 17/0, ops-for-networks 5/1.

**Tooling / CLI / CRDs** — all PowerCLI module names, both meta-module versions and build numbers, both meta-module deltas (`VMware.Vcf.Sso` added / `VMware.PowerCLI.VCenter` removed) with the "0 deprecated, 0 deleted" contradiction carried through as unresolved; `Set-PowerCLIConfiguration -InvalidCertificateAction` value set with the Linux/macOS restriction; the 5 named 9.1 cmdlets (`Get-VsanEffectiveCapacity`, `Remove-Sddc{Cluster,Domain,Host}`, `Get-SddcTask`) and the per-module counted-but-unnamed changelog table; `kubectl get {virtualmachineclass, kr, kubernetesreleases, cluster, clusterclass}`; the VM Operator `v1alpha2/v1alpha3` (VCF 9.0 docs) vs `v1alpha5` (upstream) conflict presented as unresolved with `kubectl api-resources` as the resolution; `run.tanzu.vmware.com` group string correctly marked UNVERIFIED; `builtin-generic-v3.4.0`/`v3.6.0`; `vcf version` / `vcf context list` / `vcf plugin`; `/suite-api/doc/swagger-ui.html` (singular `doc`, [S12b]).

**Honest-gap handling** — every `UNVERIFIED` marker sampled (VCF Automation All Apps token endpoint, VCF Installer auth, realtime-metrics token, ports matrix, NSX 9.1 auth prose 429, precheck enumeration, depot payload shape, upgrade orders 2–5 and 9–23, fate of 9.0 appliances, IAM defaults vs maxima, `Get-PowerCLIConfiguration`, CEIP parameter, offline install) traces to a real `## Gaps and Ambiguities` item or retrieval failure in the corresponding dossier. Nothing marked UNVERIFIED in a dossier was upgraded to verified in a skill — **except** the three NSX spec-example fabrications and the vCenter session-path claim listed above, which move in the opposite direction (asserting spec support that does not exist).

**Doc-portal claims** — all 15 `developer.broadcom.com` slugs, the `nsx-t-data-center-rest-api` legacy slug, the `vmware-cloud-foundation-api`=5.2.4 trap, the `opnapi-for-sddc-manager` typo, the `vr-ops`/`vrealize-operations-manager` legacy slugs, the release-notes path divergence table, the 5.2-era 9.1 slugs, the SPA-shell-on-200 and 429 failure signatures — all trace to `research/SOURCE-INVENTORY.md` or the per-dossier source indexes.
